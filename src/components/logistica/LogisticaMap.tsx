// Re-export the lazy-loaded map component for backwards compatibility
import { LazyLogisticaMap } from './LazyLogisticaMap';

export { LazyLogisticaMap as LogisticaMap };
export { LazyLogisticaMap };
export default LazyLogisticaMap;
